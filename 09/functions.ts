const numero: number = 1
const numero_1: number = 2

function sumar(numero: number, numero_1: number): number {
    return numero + numero_1
}

function cli(tex: string): void {
    if (tex == 'hola') {
        console.log('Hola Mundo')
    } else {
        console.log('Chau mundo')

    }

}

function resta(numero: number, numero_1: number): number {
    return numero - numero_1
}